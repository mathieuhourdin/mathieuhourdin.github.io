---
export interface Props {
  progress?: number
}

const { progress = 0 } = Astro.props

const color = (() => {
  if (progress > 70) return 'bg-green-400'
  if (progress > 50) return 'bg-orange-400'
  return 'bg-red-400'
})()
---

<div class="bg-gray-200 m-2 w-1/3">
  <div
    title="Cette barre représente le niveau d'avancement de l'écriture de l'article"
    class={`${color} h-2`}
    style={`width: ${progress}%`}
  >
  </div>
</div>
